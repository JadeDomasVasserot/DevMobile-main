{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idcommande</th>
                <td>{{ commande.idcommande }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ commande.date ? commande.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ commande.number }}</td>
            </tr>
            <tr>
                <th>Amountoutmargin</th>
                <td>{{ commande.amountoutmargin }}</td>
            </tr>
            <tr>
                <th>Commargin</th>
                <td>{{ commande.commargin }}</td>
            </tr>
            <tr>
                <th>Totalamount</th>
                <td>{{ commande.totalamount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_user_index') }}">back to list</a>

    <a href="{{ path('commande_user_edit', {'idcommande': commande.idcommande}) }}">edit</a>

    {{ include('commande_user/_delete_form.html.twig') }}
{% endblock %}
