<form method="post" action="{{ path('category_delete', {'idcat': category.idcat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.idcat) }}">
    <button class="btn">Delete</button>
</form>
