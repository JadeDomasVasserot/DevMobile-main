<form method="post" action="{{ path('products_in_basket_delete', {'idpib': products_in_basket.idpib}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ products_in_basket.idpib) }}">
    <button class="btn">Delete</button>
</form>
