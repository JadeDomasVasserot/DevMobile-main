{% extends 'base.html.twig' %}

{% block title %}ProductsInBasket{% endblock %}

{% block body %}
    <h1>ProductsInBasket</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idpib</th>
                <td>{{ products_in_basket.idpib }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ products_in_basket.quantity }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ products_in_basket.size }}</td>
            </tr>
            <tr>
                <th>Shoessize</th>
                <td>{{ products_in_basket.shoessize }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('products_in_basket_index') }}">back to list</a>

    <a href="{{ path('products_in_basket_edit', {'idpib': products_in_basket.idpib}) }}">edit</a>

    {{ include('products_in_basket/_delete_form.html.twig') }}
{% endblock %}
